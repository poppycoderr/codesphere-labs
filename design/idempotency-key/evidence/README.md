# 证据说明

全部由 `make evidence`（即 `scripts/verify.sh evidence`）生成。

| 文件 | 内容 |
|---|---|
| `output.tsv` | 每个场景的结果 |
| `final-tables.txt` | 幂等记录与报名表的最终内容（每个键的状态、token、写入次数与执行者） |
| `container.txt` | 镜像与驱动版本 |
| `environment.txt` | 运行环境 |

规范化：无。
