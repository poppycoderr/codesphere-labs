# 证据说明

全部由 `make evidence`（即 `scripts/verify.sh evidence`）生成。

| 文件 | 内容 |
|---|---|
| `output.tsv` | 每个场景的结果 |
| `client.log` | 客户端标准错误（去掉 SLF4J 提示） |
| `container.txt` | 镜像与客户端版本 |
| `environment.txt` | 运行环境 |

规范化：无。
