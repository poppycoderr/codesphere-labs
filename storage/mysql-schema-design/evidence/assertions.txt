通过：唯一索引包含可空的 deleted_at：两条未删除记录都插入成功
通过：deleted_id 写法：两次退订后 id 1、2 的 deleted_id 为自身主键，id 3 为 0
通过：deleted_id 写法：再插入有效记录报唯一键冲突
通过：函数索引写法：再插入有效记录报唯一键冲突
通过：10.1.0.0/16：二进制 3916 行，字符串 BETWEEN 2672 行（少 1244 行，32%），LIKE 3916 行
通过：二进制可以查 /20 网段：247 行
通过：'10.1.100.1' < '10.1.99.1' 在字符串比较中成立，二进制比较中不成立
通过：idx_ip：二进制 16.5MB，字符串 26.6MB
通过：同一行：1 线程 1793 TPS，64 线程 2216 TPS，不到 2 倍
通过：同一行 16 线程：几乎每次更新都经历锁等待（6399 / 6400）
通过：同一行 64 线程：几乎每次更新都经历锁等待（25599 / 25600）
通过：64 线程分散到 1,000 行：16420 TPS，是同一行的 7.4 倍
通过：64 线程分桶：4 桶 2531 TPS（1.1×），16 桶 5300 TPS（2.4×）
