<!-- 由 scripts/summarize.py 生成；缺失 = 客户端收到成功确认、但新 source 上不存在的序号 -->

| 场景 | 已确认写入 | 新 source 上存在 | 缺失 | 重复 | 故障→判定 | 判定→提升完成 | 提升→路由切换 | 故障→第一笔新写入 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `async-partition` | 345 | 208 | 137 | 0 | 6.6s | 0.6s | 0.2s | 8.6s |
| `semisync-applier-paused` | 391 | 391 | 0 | 0 | 6.4s | 2.3s | 0.2s | 9.8s |
| `semisync-timeout` | 457 | 202 | 255 | 0 | 6.6s | 2.1s | 0.2s | 9.8s |

通过：异步复制：分区 3 秒后 source 崩溃，137 笔已确认写入在新 source 上不存在
通过：缺失的写入都在分区开始前 0.5 秒之后、崩溃之前得到确认（最早一笔比分区开始早 0ms：已确认但还在复制途中）
通过：三个场景都没有重复写入（request_id 唯一）
通过：崩溃时正在执行的那一笔：新 source 上不存在，按同一 request_id 重试后写入
通过：旧 source 重启后多出 125-261 这些事务，数量正好等于丢失的写入
通过：直接接回后 SQL 线程报错停止：Worker 1 failed executing transaction '683c7534-b77a-11f1-8a75-a6e2a55d5d57:1' at source log binlog.000001, en…
通过：两边 orders 已经分叉：旧 source 251 行，新 source 208 行
通过：半同步在故障前一直有效（yes_tx 289，no_tx 0）
通过：replica1 已 ACK 但暂停应用：如果不先应用 relay log 就提升，会缺 277 笔已确认写入
通过：先应用完已接收的 relay log 再提升：缺失 0 笔
通过：候选比较时 replica1 的已接收集合大于已执行集合
通过：两个 replica 都不可达、等待 2 秒超时后半同步退化为异步（no_tx 164）
通过：退化期间确认的写入在崩溃后缺失 255 笔
通过：两笔写入都在等待半同步 ACK 时触发客户端 1 秒超时
通过：没有唯一键：按同一请求重试后出现 2 行（原请求其实已提交）
通过：有唯一 request_id：重试命中已存在的行，affected_rows = 0，仍是 1 行
通过：复制正常的 replica1：写完立即读，300/300 次读不到
通过：读 source、或在 replica 上等待写入的 GTID：全部读到
通过：GTID 等待的代价：replica1 中位 0.79ms，延迟 1 秒的 replica2 中位 2010.52ms
通过：延迟 1 秒的 replica2：直接读全部读不到
