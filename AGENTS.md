# codesphere-labs 协作约定

本文件写给在本仓库工作的 AI 编码代理（Claude Code、Codex 等），人工贡献者遵循同样的规则。

本仓库是 [codesphere](https://github.com/poppycoderr/codesphere) 技术文章的伴生实验仓库：保存可运行示例、验证脚本和原始证据。修改前先读：

- [README.md](README.md)：实验列表与目录约定
- [docs/verification-standard.md](docs/verification-standard.md)：验证级别、黄金样板与轻量实验、完成定义
- [docs/evidence-format.md](docs/evidence-format.md)：`experiment.yaml` 字段、证据目录与规范化规则
- [docs/article-integration.md](docs/article-integration.md)：文章与实验的同步流程
- [CONTRIBUTING.md](CONTRIBUTING.md)：脚本约定与性能数据的写法

## 核心规则

1. **先跑实验，再写结论。** 结论来自 `evidence/` 中的原始输出，不挑选支持预设结论的输出；实验结果与文章不一致时改文章。
2. **一条入口。** 每个实验用 `make verify` 完成运行与断言，失败返回非零退出码；`make evidence` 重新采集证据。
3. **不提交未经验证的空壳。** 没有跑通并归档证据的实验不建目录。
4. **不泄露环境信息。** 不写用户名、主机名、个人绝对路径、真实密码和 Token；提交前运行 `make check`。
5. **性能数据只断言相对关系。** 写清预热、采样次数、并发度、统计口径和资源限制，不用严格耗时阈值判断成败。
6. **容器隔离。** compose 项目名使用 `csl-` 前缀，不暴露宿主机端口，`make clean` 只删除本实验的资源。

## 分支与提交

- 本仓库使用下文共享规则中的**直接提交模式**：提交到 `main`，不建功能分支，不开 PR。
- scope 使用专题名，例如 `feat(storage): ...`、`fix(java): ...`；仓库级改动用 `build`、`ci`、`docs`，不带 scope。
- 推送前运行 `make check`，改动涉及的实验要 `make verify` 通过。
- codesphere 文章通过固定 commit 引用本仓库，改写历史会让这些链接失效；确需改写时同步更新 codesphere 中的链接。

## 文档语言

实验文档和验证记录使用中文，与 codesphere 保持一致；代码标识符、提交信息使用英文。

<!-- shared-agent-rules:start v1 — 本段在 codesphere、codesphere-labs、domain-driven-kit、grounded-access 中按语言保持一致 -->
## 共享规则

以下规则由作者的四个仓库共用：codesphere、codesphere-labs、domain-driven-kit、grounded-access。本文件中的项目专属章节是对它的补充；两者冲突时，以项目专属章节为准。

### 交流语言

- 与作者交流使用中文；只有中文无法准确表达时（报错原文、标识符、引用）才用英文。
- 提交信息、PR 标题、代码标识符和日志使用英文。文档使用仓库本身的语言。

### 高效工作

- 动手前先读相关代码和文档；涉及多个文件的改动一次规划好，不要改一步问一次。
- 搜索要收窄范围：按具体路径 grep，只读需要的行段，刚编辑过的文件不必重新读取。
- 用局部替换修改文件，不整份重写；不要把文件内容再贴回对话。
- 互不依赖的命令放在一起执行；耗时的构建或实验放到后台运行，不要轮询。
- 已经定下的处理方式（已知报错、权限流程、工具选择）直接沿用，不再重复解释。
- 完成后简短汇报：改了什么、如何验证、提交 hash 和推送结果。除非被问到，不复述思考过程。
- 遇到问题先自行修复重试。只有风险高或不可逆的操作才停下来确认：删除数据、强制推送、改写历史、发布版本，以及任何涉及生产环境的操作。

### 代码

- 遵循已有的代码风格和目录结构。没有明确理由不引入新框架或工具链，不改动与任务无关的文件。
- 注释：类级注释说明这个类型的用途；方法注释只写坑点，例如并发、顺序、不直观的算法；不写逐行注释和 TODO 占位。
- Java：4 空格缩进，每行约 150 列以内，record 每个组件单独一行。
- 第三方库和 API 的用法先查文档，不凭记忆；依赖和镜像固定版本。
- 测试覆盖核心逻辑和失败路径，不追求覆盖率数字。不得为了让构建变绿而跳过、禁用或放宽检查。
- 只汇报实际跑过的测试、基准和测量结果，证据放在项目规定的位置。

### 提交

- 使用英文 Conventional Commits：`<type>(<可选 scope>): <subject>`，subject 为小写祈使句。只写一行，不写正文。一个提交只做一件事。
- 不加 emoji、不加 AI 署名、不加 `Co-Authored-By` 尾注。
- 提交前检查暂存区：不混入草稿、构建产物、密钥或无关改动。不得用 `git add -f` 提交被忽略的路径。
- 只在作者要求时提交和推送。未经明确同意，不强制推送、不改写已推送的历史。
- 推送因认证过期失败时，直接执行 `gh auth login` 后继续，只汇报结果。

### 分支与 PR

每个仓库属于以下两种模式之一，由项目专属章节说明。

- **直接提交模式**（codesphere、codesphere-labs）：直接提交到默认分支，不建功能分支，不开 PR。
- **PR 模式**（domain-driven-kit、grounded-access）：
  - 不直接提交到 `main`。从最新的 `main` 拉分支，命名为 `<type>/<简短描述>`，例如 `feat/policy-compiler`、`docs/threat-model`；不使用 `codex/`、`claude/` 等工具前缀。
  - 一个分支只包含它自己的改动。冲突通过 rebase 到 `main` 解决，合并方式只用 rebase。未经明确指示，不合并 PR、不发布版本、不执行部署。
  - **PR 标题**：英文，格式与提交信息相同。
  - **PR 描述**：中英双语。先写完整的英文版，然后一条分隔线（`---`），再写内容相同的中文版。只写改动本身：背景、范围、契约或数据模型变化、关键设计决策；不写「测试」「风险」「部署」等章节（测试结果看 CI），不加 AI 署名页脚。功能的第一个 PR 按全新功能来写，不罗列开发过程中修过的问题。
  - **PR 图示**：每个 PR 1—3 张，只选能说明问题的最小视图（浅层目录树、调用树或 `mermaid` 时序图、`diff`、伪代码），用 `text`、`diff` 或 `mermaid` 代码块写在对应文字旁边。图放在英文部分，中文部分引用即可。

### CI 与自动化

- 推送和 PR 自动触发的检查让它自行运行，不取消、不重跑、不禁用。
- 只在被要求时手动触发工作流，触发后返回运行链接；除非被要求等待或排查，不轮询结果。

### 草稿与敏感信息

- 计划、评审、笔记和可视化产物放在仓库已忽略的草稿目录（codesphere 为 `drafts/`，其他仓库为 `.agentdocs/`），不写进受版本管理的文档，也不修改忽略规则来提交它们。
- 代码、日志、证据和文档中不出现密码、Token、内部域名、个人绝对路径和真实业务数据；使用 `example_password` 这类明显的演示值。
<!-- shared-agent-rules:end -->
