# 证据说明

全部由 `make evidence`（即 `scripts/verify.sh evidence`）生成。

| 文件 | 内容 |
|---|---|
| `output.tsv` | 五组对照的结果（键、事实） |
| `inheritance-plans.txt` | 三种继承映射下两个查询的 SQL 与 `EXPLAIN ANALYZE` |
| `container.txt` | MySQL 镜像、JDBC 驱动与 JDK 镜像 |
| `environment.txt` | 运行环境 |
